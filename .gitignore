# 参考项目，仅本地参考，不纳入版本控制
ref/

# 用户私有配置（含 API key 等敏感信息）；用 config.example.yaml 作模板
config.yaml

# arxo 运行时数据
.arxo/
notes/

# arxo init 生成的本机软链（含绝对路径，每台机器自行 `arxo init` 重建）
.claude/

# Python
__pycache__/
*.py[cod]
*.egg-info/
.eggs/
build/
dist/
.venv/
venv/
env/

# 缓存/临时
*.tmp
.DS_Store
